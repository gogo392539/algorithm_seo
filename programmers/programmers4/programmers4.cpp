// programmers4.cpp : 이 파일에는 'main' 함수가 포함됩니다. 거기서 프로그램 실행이 시작되고 종료됩니다.
//

// 문제 : 주식가격(LEVEL 2)

#include "pch.h"
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

void printVector(vector<int> vData);
vector<int> solution(vector<int> prices);

int main()
{

	return 0;
}

vector<int> solution(vector<int> prices) 
{
	vector<int> answer;




	return answer;
}

void printVector(vector<int> vData)
{

	for (int i = 0; i < vData.size(); i++) {
		cout << vData[i] << endl;
	}

}